# 03_画面設計

ページ（画面）ごとの詳細設計を置くフォルダ。

画面一覧と画面遷移図は [01_全体設計/01_サイトマップ・画面一覧](../01_全体設計/01_サイトマップ・画面一覧.md) にまとめてある（そちらが正本）。

## 使い方

1. [\_テンプレート.md](./_テンプレート.md) をコピーし、`SCR-XXX_画面名.md` という名前で保存する
   - 例: `SCR-001_トップ.md`, `SCR-010_商品一覧.md`
   - 番号は機能のまとまりごとに 10 ずつ空けると、後から画面を追加しやすい
2. [画面一覧](../01_全体設計/01_サイトマップ・画面一覧.md#2-画面一覧) に 1 行追加し、このファイルへのリンクを張る
3. 画面遷移が変わったら、同じファイルの画面遷移図を更新する
4. 全画面に共通するもの（ヘッダー、認証、エラー方針など）は `02_共通設計` に書き、各画面からはリンクだけ張る

## 実装の見本（メモ・TODO）

アプリの機能ではなく、このテンプレートでの実装の見本として置いている画面。新しい画面を作るときは、作りたいものに近い見本を真似する。2 つの見本は、見せることが重ならないように役割を分けている。

| 見せたいこと | 見本 | 画面 |
| ------------ | ---- | ---- |
| 一覧（読み取り。CQRS の query）、キャッシュ（`'use cache'`） | メモ | [SCR-010 メモ一覧](./SCR-010_メモ一覧.md) |
| ブラウザ側での取り直し（TanStack Query）、画面用の API | メモ | [SCR-011 メモ検索](./SCR-011_メモ検索.md) |
| 登録（更新。CQRS の command）、フォームと入力エラー、データを見る業務のルール（重複禁止） | メモ | [SCR-012 メモ登録](./SCR-012_メモ登録.md) |
| URL での絞り込み（`searchParams` ＋ `<Suspense>`）、一覧の行ごとの操作、画面の即時反映（`useOptimistic`） | TODO | [SCR-030 TODO 一覧](./SCR-030_TODO一覧.md) |
| 状態を持つエンティティ（完了にする・元に戻す）、変更・削除（取り出す → エンティティのメソッド → 保存）、1 件の画面（`[id]` ＋ `<Suspense>`、見つからなければ 404） | TODO | [SCR-031 TODO 詳細](./SCR-031_TODO詳細.md) |

- 本来の画面を作り始めたら、見本を消すか残すかを決める（各画面の「9. 未決事項」）
