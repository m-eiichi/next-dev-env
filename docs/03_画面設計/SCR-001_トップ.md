# SCR-001 トップ

| 項目 | 内容 |
| ---- | ---- |
| 画面ID | SCR-001 |
| 画面名 | トップ |
| ステータス | レビュー待ち |
| 作成者 / 更新日 | / 2026-09-24 |

## 1. 概要

### 1.1 目的

このテンプレートの概要（技術スタック、層の分け方、始め方）を一目で分かるようにする。詳しい内容は `docs/` の資料を読んでもらう。

### 1.2 ルーティング

| 項目 | 内容 |
| ---- | ---- |
| URL パス | `/` |
| 実装ファイル | `src/app/page.tsx` |
| レイアウト | `src/app/layout.tsx` |
| 動的パラメータ (`params`) | なし |
| クエリ (`searchParams`) | なし |

### 1.3 アクセス権限

| 項目 | 内容 |
| ---- | ---- |
| 閲覧できるユーザー | 全員 |
| 権限がない場合 | なし |
| チェックする場所 | なし |

### 1.4 メタデータ

| 項目 | 内容 |
| ---- | ---- |
| title | `Next.js 開発テンプレート`（ルートレイアウトの `title.default`） |
| description | ルートレイアウトの `description` |
| 生成方法 | page では書かず、ルートレイアウトの `metadata` を使う |
| OGP / robots | なし |

## 2. 画面レイアウト

```
+--------------------------------------+
| Header（共通）                         |
+--------------------------------------+
| [A] 見出しと説明文、サンプル一覧へのリンク   |
| [B] 技術スタック（カードの一覧）           |
| [C] アーキテクチャ（層のカード。押すと SCR-020） |
| [D] 始め方（手順とコマンド）               |
+--------------------------------------+
| Footer（共通）                         |
+--------------------------------------+
```

### 2.1 レスポンシブ

| ブレークポイント | レイアウトの違い |
| ---------------- | ---------------- |
| モバイル (< `sm`) | [B] は 1 列。[C] は層名・パス・説明を縦に並べる |
| `sm` 以上 | [B] は 2 列。[C] は横 1 行に並べる |
| `lg` 以上 | [B] は 3 列 |

## 3. コンポーネント構成

| 記号 | コンポーネント | ファイル | 種別 | 備考 |
| ---- | -------------- | -------- | ---- | ---- |
| - | Page | `src/app/page.tsx` | Server | データ取得はない |
| A | HeroSection | `src/app/_components/hero-section.tsx` | Server | |
| B | TechStackSection | `src/app/_components/tech-stack-section.tsx` | Server | |
| C | ArchitectureSection | `src/app/_components/architecture-section.tsx` | Server | 各層のカードは SCR-020 へのリンク |
| D | GettingStartedSection | `src/app/_components/getting-started-section.tsx` | Server | |

- 操作がないので、Client Component は使わない

## 4. 表示項目

| No | 項目名 | 記号 | データの出どころ | 表示形式 | 値がないとき | 備考 |
| -- | ------ | ---- | ---------------- | -------- | ------------ | ---- |
| 1 | 技術名・バージョン・用途 | B | コンポーネント内の定数（[02 システム構成](../01_全体設計/02_システム構成.md) の「1. 技術スタック」を写したもの） | テキスト | - | 資料を変えたら合わせて直す |
| 2 | 層の名前・パス・役割 | C | `src/app/_components/architecture-layers.ts` の定数。SCR-020 と共用（[06 アーキテクチャ](../01_全体設計/06_アーキテクチャ.md) の「2. 層の構成」を写したもの） | テキスト | - | 同上 |
| 3 | 手順・コマンド | D | コンポーネント内の定数（ルートの `README.md` の「始め方」を写したもの） | コードブロック | - | 同上 |

## 5. 入力項目

なし

## 6. 操作・イベント

| No | きっかけ | 処理内容 | 成功したとき | 失敗したとき |
| -- | -------- | -------- | ------------ | ------------ |
| 1 | 画面を開く | なし（静的な表示） | 画面を表示 | - |
| 2 | [サンプル一覧を見る] を押す | 画面を移動 | SCR-010 を表示 | - |
| 3 | [C] の層のカードを押す | 画面を移動 | その層の SCR-020 を表示 | - |

### 6.1 遷移先

| 遷移先 | きっかけ | 方法 |
| ------ | -------- | ---- |
| SCR-010 サンプル一覧 | [A] の [サンプル一覧を見る] を押す | `<Link href="/example">` |
| SCR-020 層の説明 | [C] の層のカードを押す | `<Link href="/architecture/{layer}">`（カード全体をリンクにする） |

- ヘッダーのサイト名は `/` へのリンク

## 7. データ取得・更新

### 7.1 取得

なし。表示する内容は固定なので、静的にレンダリングする。

### 7.2 更新

なし

## 8. 状態ごとの表示

なし（取得も更新もないため）

## 9. 未決事項

| No | 内容 | 担当 | 期限 | 結論 |
| -- | ---- | ---- | ---- | ---- |
| 1 | 要件定義（[01 概要](../00_要件定義/01_概要.md)）が決まったら、トップに載せる内容をアプリ本来のものに置き換える | | | |

## 10. 変更履歴

| 日付 | 変更内容 | 変更者 |
| ---- | -------- | ------ |
| 2026-09-23 | 新規作成 | |
| 2026-09-23 | [A] にサンプル一覧（SCR-010）へのリンクを追加 | |
| 2026-09-24 | [C] の各層のカードを SCR-020 層の説明へのリンクにした。層の情報を `architecture-layers.ts` に移した | |
