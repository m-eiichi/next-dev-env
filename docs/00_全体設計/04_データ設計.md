# 04 データ設計

<!-- テーブルが増えて 1 ファイルに収まらなくなったら、04_データ設計/ フォルダに分ける -->

## 1. データの保存先

| 保存先 | 保存するデータ | 備考 |
| ------ | -------------- | ---- |
| DB | | |
| ファイルストレージ | 画像など | |
| 外部サービス | | [05 外部連携](./05_外部連携.md) を参照 |

## 2. ER 図

```mermaid
erDiagram
  %% 例:
  %% USER ||--o{ POST : writes
  %% USER {
  %%   string id PK
  %%   string email
  %%   datetime created_at
  %% }
```

## 3. エンティティ一覧

| エンティティ | テーブル名 | 概要 |
| ------------ | ---------- | ---- |
| ユーザー | `users` | |

## 4. テーブル定義

### 4.1 `users`（例）

| カラム | 型 | NULL | 初期値 | キー | 説明 |
| ------ | -- | ---- | ------ | ---- | ---- |
| `id` | | 不可 | | PK | |
| `created_at` | timestamp | 不可 | 現在時刻 | | |
| `updated_at` | timestamp | 不可 | 現在時刻 | | |

- インデックス:
- 制約:

## 5. 共通ルール

| 項目 | ルール |
| ---- | ------ |
| 命名 | テーブルは複数形の snake_case、カラムは snake_case など |
| 主キー | 連番 / UUID / ULID |
| 日時 | タイムゾーン（UTC で保存し、表示するときに変換するなど） |
| 削除 | 物理削除 / 論理削除（`deleted_at`） |
| マイグレーション | 使うツールと、実行する手順 |

## 6. 変更履歴

| 日付 | 変更内容 | 変更者 |
| ---- | -------- | ------ |
| YYYY-MM-DD | 新規作成 | |
