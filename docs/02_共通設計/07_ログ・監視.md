# 07 ログ・監視

<!-- 運用の方針（保持期間など）は 00_要件定義/03_非機能要件 に書く。ここは実装のルール -->

## 1. ログの出し方

| 項目 | ルール |
| ---- | ------ |
| 使うもの | `console` / ロガーライブラリ（pino など） |
| 形式 | テキスト / JSON（本番環境は JSON がおすすめ） |
| 出力先 | ホスティングのログ機能 / 外部サービス |

## 2. ログレベル

| レベル | 使う場面 | 例 |
| ------ | -------- | -- |
| error | 想定外のエラー。対応が必要 | DB 接続の失敗 |
| warn | 想定内だが注意が必要 | 外部 API のリトライ |
| info | 重要な操作の記録 | ログイン、データの作成・削除 |
| debug | 開発中の調査用。本番では出さない | |

## 3. 出してはいけない情報

- パスワード、トークン、API キー
- クレジットカード番号などの決済情報
- 必要のない個人情報（メールアドレスや住所は、マスクするか ID に置き換える）

## 4. エラー監視

| 項目 | 方針 |
| ---- | ---- |
| サービス | Sentry など / 使わない |
| サーバー側のエラーの受け取り | `src/instrumentation.ts` の `onRequestError` |
| ブラウザ側のエラーの受け取り | `src/instrumentation-client.ts` |
| 通知先 | Slack / メールなど |

## 5. 未決事項

| No | 内容 | 担当 | 期限 | 結論 |
| -- | ---- | ---- | ---- | ---- |
| 1 | Route Handler（`src/server/entry/api/`）は想定外のエラーを中で捕まえて `500`（`INTERNAL_ERROR`）を返す（[09 外部公開 API](./09_外部公開API.md) の「6」「9」）。そのため、エラーが `onRequestError` まで届かず、今は `console.error` でログに出すだけになっている。エラー監視のサービスを入れるときに、Route Handler の中からも送るようにする（例: `problemResponse()` で `500` を返す前に送る） | | エラー監視のサービスを決めるとき | |

## 6. 変更履歴

| 日付 | 変更内容 | 変更者 |
| ---- | -------- | ------ |
| YYYY-MM-DD | 新規作成 | |
| 2026-09-23 | 「5. 未決事項」を追加（Route Handler の想定外のエラーがエラー監視に届かない件） | |
